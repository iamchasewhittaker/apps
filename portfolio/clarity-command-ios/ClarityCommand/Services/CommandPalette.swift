import SwiftUI
import ClarityUI

enum CommandPalette {
    // Gold accent — Clarity Command's brand color (#c8a84b)
    static let accent = Color(red: 0.784, green: 0.659, blue: 0.294)
    static let accentLight = Color(red: 0.133, green: 0.114, blue: 0.055)

    // Purple for "Her Words" card
    static let purple = Color(red: 0.753, green: 0.522, blue: 0.988)
    static let purpleLight = Color(red: 0.102, green: 0.063, blue: 0.125)

    // Everything else comes from ClarityPalette directly
}
