# Spend Radar Web

Read-only dashboard for Spend Radar subscription/receipt data.

## Stack
- React (CRA) + localStorage cache
- Reads Google Sheet published CSV

## Run locally
```bash
npm install && npm start
```
