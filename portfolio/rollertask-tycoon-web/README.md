# RollerTask Tycoon Web

Points-based task motivation tracker (standalone, split from Clarity Hub).

## Stack
- React (CRA) + localStorage + Supabase sync

## Run locally
```bash
npm install && npm start
```
