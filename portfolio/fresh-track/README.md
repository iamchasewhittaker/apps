# Fresh Track

Grocery freshness tracker — receipt scanning, expiration dates, printable fridge plans.

## Stack
- React (CRA) + localStorage
- Dark mode only (`#09090b` base)

## Run locally
```bash
npm install && npm start
```
