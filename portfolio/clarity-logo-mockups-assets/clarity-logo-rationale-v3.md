# Clarity iOS Logo Mockups — Rationale (V3)

## Time (final)

- **Asset:** `clarity-time-icon-canonical.png`
- **Rule:** The clock-hand center (`draw_time_center` in `generate_clarity_icons.py`) is the approved mark — unchanged from the v2 Time you liked. Rings + blue badge match the rest of the family.

## Other four (iteration pass)

| App | V3 change |
|-----|-----------|
| **Check-in** | Simpler EKG with fewer peaks; single green wellness dot at the key spike instead of a heavy arc. |
| **Triage** | Replaced funnel-with-bars with a **three-tier priority stack** (wide → narrow) plus a **down chevron** for sort/order. Amber accent only on the top bar (capacity / “now”). |
| **Budget** | Same envelope metaphor, **two ledger stripes** under the flap (dropped one bar vs v2) for less clutter. |
| **Growth** | **Straight two-segment “rise”** with a bolder arrowhead; sprout stays as a small green ring + stem at the origin. |

## Bundle files

- `clarity-homescreen-mockup-v3.png` — five icons with **canonical** Time in the Time slot.
- `clarity-icon-comparison-board-v3.png` — same set on dark + light strips.

v2 assets are still generated by the script for A/B comparison.
