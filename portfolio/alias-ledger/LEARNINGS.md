# Learnings — Alias Ledger

## 2026-04-18
- Single-file static HTML deploys to Vercel instantly — no `package.json`, no build step, no config needed
- File must be named `index.html` for Vercel to serve it as the root without a `vercel.json`
