# Alias Ledger

Hide My Email alias tracker. Single-file HTML app, no build step.

## Stack
- HTML/JS + localStorage
- Deployed: alias-ledger.vercel.app

## Run locally
Open `index.html` in a browser.
